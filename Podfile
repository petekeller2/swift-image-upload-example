# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'image-upload-example' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for image-upload-example
  pod 'Alamofire', '~> 4.0'
  pod 'Material', '~> 2.0'
  pod 'Gallery'
  pod 'Lightbox'
  pod 'Hue'
  pod 'SVProgressHUD'
  pod 'Toast-Swift', '~> 4.0.0'
  pod 'IJProgressView', :git => 'https://github.com/petekeller2/IJProgressView.git'

  target 'image-upload-exampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'image-upload-exampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
